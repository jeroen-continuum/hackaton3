fastapi>=0.115
uvicorn[standard]>=0.30
sqlmodel>=0.0.22
pydantic-settings>=2.4
httpx>=0.27
python-dotenv>=1.0
anthropic>=0.40
pandas>=2.2
psycopg[binary]>=3.2
pytest>=8.0
respx>=0.22
# Optional: only needed when ENABLE_WEB_CRAWL=true (run `crawl4ai-setup` once
# afterwards to install the headless Chromium it drives).
crawl4ai>=0.4
